<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xiaozhi.modules.device.dao.DeviceDao">
    <!-- 获取此智能体全部设备的最后连接时间 - MySQL -->
    <select id="getAllLastConnectedAtByAgentId" resultType="java.util.Date" databaseId="mysql">
        SELECT last_connected_at FROM ai_device
        WHERE agent_id = #{agentId}
        ORDER BY last_connected_at DESC LIMIT 0, 1
    </select>

    <!-- 获取此智能体全部设备的最后连接时间 - PostgreSQL -->
    <select id="getAllLastConnectedAtByAgentId" resultType="java.util.Date" databaseId="postgresql">
        SELECT last_connected_at FROM ai_device
        WHERE agent_id = #{agentId}
        ORDER BY last_connected_at DESC NULLS LAST LIMIT 1
    </select>
</mapper> 